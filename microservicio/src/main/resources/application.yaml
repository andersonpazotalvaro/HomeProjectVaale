server:
  port: 8001
  servlet:
    context-path: /home

spring:
  servlet:
    multipart:
      enabled: true

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/home?useSSL=false
    username: root
    #password: 12345678
    password:

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    #properties:
     # hibernate:
      #  dialect: org.hibernate.dialect.MySQLDialect
