server:
  port: 8001
  servlet:
    context-path: /home

spring:

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/home?useSSL=false
    username: root
    password: 12345678

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create

